version: '3'
services:
  frontend:
    build: ./frontend
    ports:
      - 3000:3000
    networks:
      - my-network

  backend:
    build: ./backend
    ports:
      - 8001:8001
    networks:
      - my-network

networks:
  my-network: